# =========================================================
# .env.example
# Plantilla pública de variables de entorno.
# Copiar este archivo a `.env` y completar valores reales.
# El archivo `.env` NUNCA debe subirse al repositorio.
# =========================================================

# ---------- Django ----------
SECRET_KEY=cambiar-esta-clave-en-produccion
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# ---------- PostgreSQL ----------
POSTGRES_DB=simulador_db
POSTGRES_USER=simulador_user
POSTGRES_PASSWORD=simulador_pass
POSTGRES_HOST=db
POSTGRES_PORT=5432
# Puerto expuesto al host (Windows) — solo para DBeaver/pgAdmin. Si tienes otro
# Postgres corriendo en 5432, cambia este. El contenedor sigue usando 5432 internamente.
POSTGRES_HOST_PORT=5435

# ---------- CORS ----------
# URLs del frontend autorizadas a consumir la API (separadas por coma)
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://127.0.0.1:4200

# ---------- JWT ----------
# Tiempo de vida del access token en minutos
JWT_ACCESS_LIFETIME_MIN=60
# Tiempo de vida del refresh token en días
JWT_REFRESH_LIFETIME_DAYS=7

# ---------- IA generativa ----------
# Provider: "openai" (por defecto) o "anthropic"
AI_PROVIDER=openai
# Modelo a usar — para OpenAI: gpt-4o-mini, gpt-4o, gpt-4-turbo, ...
# Para Anthropic: claude-3-5-sonnet-latest, claude-3-5-haiku-latest, ...
AI_MODEL=gpt-4o-mini
# Temperatura 0.0–1.0 (más alto = más creativo, menos predecible)
AI_TEMPERATURE=0.7
# Una sola es necesaria — la del provider elegido.
# Si no se configura ninguna, el backend devolverá un stub mejorado y marcará
# la propuesta como generada_con_llm=False para que el frontend lo avise.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ---------- Correo (notificaciones a estudiantes, fases posteriores) ----------
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=no-reply@simulador.local
