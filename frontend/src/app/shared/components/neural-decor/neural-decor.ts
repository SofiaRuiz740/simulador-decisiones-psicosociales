import { Component } from '@angular/core';

/** Decoración SVG abstracta (líneas neuronales) — no interactiva, aria-hidden. */
@Component({
  selector: 'app-neural-decor',
  templateUrl: './neural-decor.html',
  styleUrl: './neural-decor.scss',
})
export class NeuralDecor {}
