/**
 * Configuración para despliegue en Docker (nginx + proxy /api).
 */
export const environment = {
  production: true,
  apiUrl: '/api',
  appName: 'Simulador de Decisiones Psicosociales',
  simulacionNarrativaDataUrl: '/simulacion-narrativa',
};
